---
title: contact 
---

import Layout from '../components/layout'

export default Layout

# Contact

This is an example of adding a theme with [Theme UI][].
Click the buttons in the header to set the theme.
The color mode should persist after reloading the page and across different pages in the site.


[theme ui]: https://theme-ui.now.sh
